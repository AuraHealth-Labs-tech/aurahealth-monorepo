[package]
name = "aurahealth_soroban_contract"
version = "0.1.0"\nedition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "0.28.0", default-features = false, features = ["testutils"] }

[profile.release]
opt-level = "z"
