{
  "name": "aurahealth-soroban-contract",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release --target wasm32-unknown-unknown",
    "test": "cargo test",
    "clean": "cargo clean",
    "deploy": "bash ./scripts/deploy-contract.sh"
  }
}
